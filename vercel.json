{
   "version": 2,
   "name": "flavorsome",
   "builds": [
      { "src": "server.mjs", "use": "@now/node" }
   ],
   "routes": [
      { "src": "/(.*)", "dest": "server.mjs" }
   ],
   "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }]
}
